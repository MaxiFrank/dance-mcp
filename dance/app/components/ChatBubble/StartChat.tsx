'use client';

const StartChat = () => {
    return (
        <div>
            <button className='btn btn-primary' onClick={() => console.log('Click')}> Start Chatting </button>
        </div>
    )
}
export default StartChat

